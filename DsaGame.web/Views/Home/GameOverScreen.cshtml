
<div class="game-over-container">
    <h1>GAME OVER</h1>
    <button class="retry-btn" onclick="window.location.reload();">
        Try Again
    </button>
    <div class="subtext">
        Better luck next time!
    </div>
</div>

<style>


    .game-over-container {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    h1 {
        font-size: 48px;
        margin-bottom: 20px;
        text-transform: uppercase;
        /* Glow effect on text */
        text-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
    }

    .retry-btn {
        background: #ff5858; /* Red/Orange button color */
        border: none;
        border-radius: 30px;
        padding: 15px 30px;
        font-size: 18px;
        font-weight: 600;
        color: #fff;
        cursor: pointer;
        text-transform: uppercase;
        transition: 0.3s ease;
        /* Subtle glow effect around the button */
        box-shadow: 0 0 15px rgba(255, 88, 88, 0.6);
    }

    .retry-btn:hover {
        background: #ff3b3b; /* Darker red on hover */
        box-shadow: 0 0 20px rgba(255, 59, 59, 0.8);
    }

    .subtext {
        margin-top: 20px;
        font-size: 14px;
        opacity: 0.8;
    }
</style>